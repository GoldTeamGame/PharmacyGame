using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UnlockItem : MonoBehaviour {

    //    public void unlockDrugSet(string drug)
    //    {
    //        Drug d = Globals.findDrug(drug, Globals.prescriptionList);
    //        if (d == null)
    //            d = Globals.findDrug(drug, Globals.overCounterList);
    //        d.isUnlocked = true;
    //    }
}
